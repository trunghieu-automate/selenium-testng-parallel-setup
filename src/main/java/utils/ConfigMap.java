package utils;

/**
 * All key for config file
 */

public enum ConfigMap {
    argsMaximized,
    argsIgnoreCertificatesError,
    argsDisablePopupBlocking,
    proxyServer,
    noProxy,
    autodetect,
    httpProxyServer,
    sslProxyServer,
}